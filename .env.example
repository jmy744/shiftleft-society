# ShiftLeft Society — Environment Variables
# Copy this to .env and fill in your values.
# NEVER commit .env to git. Add .env to .gitignore.

# Qwen through an OpenAI-compatible provider. The hosted demo uses OpenRouter.
# Keep the key in your local .env or hosting provider's secret manager; never
# commit or screenshot a real key.
QWEN_API_KEY=sk-or-v1-your-openrouter-key-here
QWEN_MODEL=qwen/qwen3.8-27b:free
QWEN_BASE_URL=https://openrouter.ai/api/v1
OFFLINE_MODE=false

# Application settings
APP_ENV=development
DB_PATH=tribunal_history.db
SHIFTLEFT_API_KEY=
ALLOWED_ORIGINS=http://localhost:8000
MAX_CODE_CHARS=100000
MAX_CONCURRENT_JOBS=4

# Optional: required only for GitHub PR integration
GITHUB_TOKEN=ghp_your_personal_access_token_here
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# Set automatically on Alibaba Cloud ECS instances
ALIBABA_CLOUD_REGION=ap-southeast-1
ECS_INSTANCE_ID=i-your-instance-id
